# Multi-state rule 413 — 5-state totalistic automaton
# Category: multi-state
# Generated from rule: reference_code/rules/413.txt

name: 413-baseline
rule: '413'
num_states: 5
description: "Multi-state rule 413 \u2014 5-state totalistic automaton"
category: multi-state
board:
- 64
- 64
neighbourhood: moore8
steps: 500
global_seed: 42
metrics:
- density
- entropy
renderer: pygame
cell_size: 8
show_grid: true
log:
  enabled: true
  output_dir: runs/413-baseline
  format: csv
  metrics_every: 1
  frames_every: null
  animation: false
  provenance: true
seed:
  type: random
  density: 0.3
  states:
  - 1
  - 2
  - 3
  - 4
